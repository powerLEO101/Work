#include<iostream>
#include<iomanip>
#include<fstream>
#include<algorithm>

int Num[10001];
int main()
{
	std::ios::sync_with_stdio(false);
	int n,k;
	std::cin>>n>>k;
	for(int i = 0;i<n;i++)
		std::cin>>Num[i];
	for(int i = 0;i<n;i++)
	for(int j = 0;j<n;j++)
	for(int k = i;k<=j;k++)
	
	return 0;
}
